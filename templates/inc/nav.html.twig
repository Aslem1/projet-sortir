<nav>
    <ul>
        <li><a href="{{ path('main_home') }}" title="Home">Home</a></li>

        {% if app.user %}
            <li><a href="{{ path('app_logout') }}" title="Logout">Logout ({{ app.user.prenom }})</a></li>
            <li><a href="{{ path('app_profil') }}" title="Home">Mon profil</a></li>
        {% else %}
            <li><a href="{{ path('app_login') }}" title="Login">Se connecter</a></li>
        {% endif %}
    </ul>
</nav>